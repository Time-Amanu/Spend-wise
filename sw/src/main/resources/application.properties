spring.application.name=sw
# 1. Connection Details
spring.datasource.url=jdbc:postgresql://localhost:5432/spendwise
spring.datasource.username=postgres
spring.datasource.password = BeautifulSoup123
spring.datasource.driver-class-name=org.postgresql.Driver

# 2. Hibernate Strategy
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
#hibernate is being lazy so this has to be fixed
# 3. Explicitly tell Spring this is the "default" profile
spring.profiles.active=default
